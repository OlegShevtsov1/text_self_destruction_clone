en:
  validation:
    message:
      wrong_message_size: 'Wrong message size, max size is %{max_size}'
  controllers:
    application:
      message_not_found: 'Message not found'
  serializer:
    request:
      already_requested: 'You already requested this message'
